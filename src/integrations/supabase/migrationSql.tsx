
// This file contains SQL migrations that have been applied to the Supabase database
// It serves as documentation only and is not used in the application

/*
ALTER TABLE post_reactions
ADD CONSTRAINT post_reactions_pkey 
PRIMARY KEY (post_id, user_id, reaction_type);
*/
